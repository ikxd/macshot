<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>macshot</title>
        <link>https://github.com/sw33tLie/macshot</link>
        <description>macshot updates</description>
<item>
    <title>v4.0.0</title>
    <pubDate>Thu, 09 Apr 2026 00:46:11 +0000</pubDate>
    <sparkle:version>19</sparkle:version>
    <sparkle:shortVersionString>4.0.0</sparkle:shortVersionString>
    <sparkle:minimumSystemVersion>12.3</sparkle:minimumSystemVersion>

    <description><![CDATA[
      <style>
        body { font-family: -apple-system, sans-serif; font-size: 13px; padding: 12px; }
        h3 { font-size: 14px; margin: 12px 0 6px 0; }
        h3:first-child { margin-top: 0; }
        ul { padding-left: 18px; margin: 4px 0; }
        li { margin: 4px 0; line-height: 1.5; }
      </style>
      <h3>Added</h3><ul> <li><b>Screen recording</b> — record MP4 or GIF with configurable FPS (up to 120fps), system audio + microphone capture, annotation drawing during recording, and mouse click highlighting. Pause/resume support with gap-free output. Audio merge dialog for dual-source recordings.</li> <li><b>Webcam overlay</b> — live camera feed bubble during recordings. Circle or rounded rectangle shape, four corner positions, three sizes. Camera device picker via right-click.</li> <li><b>Keystroke overlay</b> — show pressed keys on-screen during recording. "Shortcuts Only" vs "All Keystrokes" modes.</li> <li><b>Recording countdown</b> — configurable delay timer before recording starts.</li> <li><b>Video editor</b> — standalone window for trimming, exporting, and uploading recorded videos. Timeline thumbnail strip, arrow key frame stepping, Copy button.</li> <li><b>Editable annotation history</b> — screenshots save raw image + annotation data alongside the composited image. Editing from history reopens with live, editable annotations. "Done" button commits changes back to the same history entry.</li> <li><b>Lasso marquee selection</b> — hold Shift and drag on empty space from any tool (including pencil/marker) to select multiple annotations at once.</li> <li><b>Shift+Click multi-selection</b> — hold Shift and click annotations to toggle them in/out of the selection. Drag moves all selected, Delete removes all selected, color changes apply to all.</li> <li><b>Annotation copy/paste (Cmd+C/V)</b> — copy selected annotations and paste as duplicates. Works with multi-selection.</li> <li><b>Annotation outlines</b> — outline toggle + color picker for arrows, lines, rectangles, ellipses, and number tools.</li> <li><b>Custom beautify backgrounds</b> — use any image as the beautify background with optional blur slider. 6 new gradient styles.</li> <li><b>Apple Translation (on-device)</b> — translate on-device using Apple's Translation framework on macOS 15+. Faster, offline, and private. Auto-detects source language.</li> <li><b>Smart marker mode</b> — OCR-based text detection snaps the marker to actual text lines. Vertical pill cursor with dynamic sizing.</li> <li><b>WYSIWYG drawing cursor</b> — pencil and marker tools show a live dot preview matching exact stroke size and color.</li> <li><b>"Open from Clipboard" hotkey</b> — configurable global hotkey to open clipboard image in editor.</li> <li><b>"Also open in Editor" option</b> — quick capture performs the action AND opens the editor.</li> <li><b>"Do nothing" Quick Capture mode</b> — capture without copying or saving; decide later via thumbnail.</li> <li><b>Customizable overlay/editor tool shortcuts</b> — all tool shortcuts configurable in Settings.</li> <li><b>Pencil smoothing modes</b> — 3-mode selector: None, Smooth (Chaikin), Refined (moving average + Chaikin with zero input lag).</li> <li><b>Beta update channel</b> — opt-in pre-release builds via Sparkle.</li> <li><b>WebP image opening</b> — open .webp files for editing via menu, Finder, or drag-to-dock.</li>  </ul><h3>Changed</h3><ul> <li><b>Text tool uses standard annotation chrome</b> — text annotations show the same selection UI as shapes (resize handles, rotate, delete). Double-click to re-enter editing.</li> <li><b>Unified Censor tool</b> — Pixelate, Blur, Solid, and Erase modes merged into a single toolbar button. Erase mode samples surrounding colors for seamless fills.</li> <li><b>Click-to-select annotations</b> — click any annotation with any tool to select it and edit properties in real time. Pencil/marker use long-press to avoid interfering with drawing.</li> <li><b>Recent Captures panel</b> — horizontal scrolling thumbnails with filter tabs, right-click context menu, drag-and-drop.</li> <li><b>Pixelate annotations</b> — now movable, resizable, and reorderable. Re-bakes from original screenshot.</li> <li><b>sRGB color profile</b> — proper pixel value conversion for accurate colors matching native macOS screenshots.</li> <li><b>Renamed Preferences to Settings</b> — all user-facing references updated.</li>  </ul><h3>Fixed</h3><ul> <li><b>Blurry screenshots on mixed-DPI setups</b> — direct CGImage extraction preserves exact pixel data regardless of display.</li> <li><b>Pre-macOS 14 capture corruption</b> — uses `CGWindowListCreateImage` on macOS 12–13 instead of raw memcpy.</li> <li><b>Capture delay on macOS Tahoe</b> — pre-warm SCShareableContent cache, reducing 2-second delay to near-instant.</li> <li><b>Focus returns to previous app after capture</b> — overlay dismiss re-activates the previously focused application.</li> <li><b>Purple selection border no longer appears in recordings</b> — UI chrome excluded from SCStream capture.</li> <li><b>Recording pause/resume produces gap-free video</b> — paused time subtracted from timestamps.</li> <li><b>Light mode toolbar readability</b> — toolbar and popovers force dark appearance for consistent contrast.</li> <li><b>Scroll capture reliability</b> — complete engine rewrite with CPU-backed frame comparison, Vision-based offset detection, and incremental stitching.</li> <li><b>Multi-select drag works in text tool mode</b> — text tool no longer blocks dragging multi-selected annotations.</li> <li><b>Pencil/marker shift-constrain</b> — straightening now anchors from where Shift was pressed, not the stroke origin.</li></ul>
    ]]></description>
    <enclosure
        url="https://github.com/sw33tLie/macshot/releases/download/v4.0.0/MacShot.dmg"
        length="5898274"
        type="application/octet-stream"
        sparkle:edSignature="Q+EyTp5nWznsP7BGxN+5UUPx+7frXBbo/V0Dr5jbTL0QbLtenU0gsifBGXw/0tQmn6kwXvVOrMjzWJbfiM+hCg=="
    />
</item>
    </channel>
</rss>
